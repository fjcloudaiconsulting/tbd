# Org-level Forecast + Budget enable/disable toggles — design

**Status:** design only, not yet implemented.
**Date:** 2026-05-17.
**Source:** workstream D of the 2026-05-17 parallel-team wave. Spec request at `~/.claude/projects/-Users-fjorge-src-pfv/memory/project_forecast_budget_enable_disable.md`.

## Goal

Define org-level toggles to enable/disable the Forecast feature and the Budget feature per org, riding the existing L4.11 feature-overrides substrate. The current `/forecast-plans` "Show details" toggle is orthogonal (it controls detail visibility within the feature) and stays as-is.

## Substrate (what already exists)

- `backend/app/auth/feature_catalog.py` — `FeatureKey` is a `Literal` union. `PlanFeatures` is the canonical Pydantic model. All existing keys default to `False` (fail-closed for AI features).
- `backend/app/services/feature_service.py:40-42` — resolver order is `defaults (all False) → plan.features → active org override`. The plan overlay and override are both additive `dict.update()` calls.
- `backend/app/auth/feature_deps.py:26-59` — `get_current_org_features` caches the full feature map on `request.state` (per-request, no Redis). `require_feature(key)` is a FastAPI dependency factory that 403s when the flag is off.
- `backend/app/auth/org_permissions.py:27-40` — `require_org_owner` dependency, the right gate for self-service owner toggles.
- `backend/app/routers/settings.py` — the existing org settings router.
- `backend/app/services/audit_service.py` — `record_audit_event(session_factory, event_type=..., ...)`. Convention: `org.config.allow_manual_balance_adjustment.set`, `admin.org.feature.set`.
- `frontend/lib/feature-catalog.ts` — `FEATURE_LABELS` record, must be kept in parity with backend catalog (drift-guard at `frontend/tests/lib/feature-catalog.test.ts`).
- `frontend/tests/fixtures/feature-catalog.json` — regenerated by `scripts/regen_feature_catalog_fixture.py` after catalog changes.

## Critical design tension

The resolver initialises `merged = {key: False for key in ALL_FEATURE_KEYS}`. All existing AI keys default `False`, which is correct for opt-in features. But `forecast.enabled` and `budgets.enabled` must default `True` (current behavior preserved for all existing orgs). Adding them to the catalog naively would give every org without a plan-features row `False` for both, a breaking regression.

Two paths considered:

**Option A (chosen).** Give `forecast_enabled` and `budgets_enabled` a `default=True` in `PlanFeatures`. The resolver's plan overlay calls `PlanFeatures.model_validate(raw).model_dump(by_alias=True)`, which returns the model's Python defaults for any key missing from the stored JSON. Even orgs on the free plan with no features JSON get `{"forecast.enabled": True, "budgets.enabled": True, ...}` from `_fetch_plan_features`, overriding the base `False` init. A self-service disable writes an `OrgFeatureOverride(value=False)` row. Re-enable deletes the row (back to plan default `True`).

**Option B (rejected).** Separate resolver path for "baseline-true" keys. Adds complexity for no behavioral gain.

### Option A caveats

- `canonicalize_features` in `plan_service.py` merges partial writes against `PlanFeatures`. Existing plans that don't store these new keys in their JSON get `True` from model defaults on read (correct).
- Admin UI for plan features (`/system/plans`) will show `forecast.enabled` and `budgets.enabled` as `true` by default. Admins can set them `false` on a plan to deny the feature to all orgs on that plan (intentional behavior, useful operator control).
- The non-standard `default=True` on these two keys must be documented in a comment on the `PlanFeatures` model so a future developer adding a new key consciously chooses the correct default.

## Architecture decisions

| Decision | Choice | Justification |
|---|---|---|
| Catalog key names | `forecast.enabled`, `budgets.enabled` | Dot-namespace matches existing `ai.*` pattern. |
| `PlanFeatures` default | `True` for both | Preserve current behavior on every existing org. |
| Self-service endpoint | `PATCH /api/v1/settings/features` in `settings.py`, gated by `require_org_owner` | Owner-only is consistent with other settings mutations. |
| Endpoint body | `{"forecast.enabled": bool, "budgets.enabled": bool}` (partial patch) | Each key is independent. |
| Override write rule | `value == plan_default` → delete any override; `value != plan_default` → upsert override | Keeps the override table small and the resolver clean. |
| Audit event | `org.feature.set` with detail `{feature_key, old_value, new_value}` | Matches existing `org.config.*.set` and `admin.org.feature.set` conventions. |
| UI surface | Two toggle switches in `/settings/organization` under a new "Features" card | Mirrors the existing `allow_manual_balance_adjustment` toggle already on the page. |
| UI save mode | Immediate mutation (no Save button) with inline saving spinner | Matches the `allow_manual_balance_adjustment` pattern. Each toggle is independent. |
| Backend enforcement | `require_feature("forecast.enabled")` / `require_feature("budgets.enabled")` as FastAPI deps on forecast/budget endpoints | Authoritative enforcement, returns 403 when off. |
| "Hide details" toggle fate | Unchanged | Orthogonal (per-user UI density vs. org-level feature existence). |
| Backwards compat | Data preserved on disable, restored on re-enable | Disable hides feature surface only; no data deleted. |
| Feature-flag delivery to frontend | Embed in `GET /me` response (next to existing `allow_manual_balance_adjustment`) | AuthProvider already fetches `/me` on mount; no extra round-trip. |

## Gate-site enumeration (11 touchpoints)

### Forecast (6 gates)

| # | Location | File | Gate type |
|---|---|---|---|
| F1 | Sidebar nav "Forecast Plans" item | `frontend/components/AppShell.tsx:73-76` | UI hide |
| F2 | `/forecast-plans` RSC page | `frontend/app/forecast-plans/page.tsx:44` | redirect to `/dashboard` when flag off |
| F3 | Dashboard "On Track" tile (`OnTrackTile`) | `frontend/app/dashboard/page.tsx:732` | UI hide + skip fetch |
| F4 | Dashboard `AccountMonthEndForecast` tile | `frontend/app/dashboard/page.tsx:783` | UI hide + skip fetch |
| F5 | Dashboard forecast chart (`forecastChartRows`) | `frontend/app/dashboard/page.tsx:561-576` | UI hide + skip fetch |
| F6 | All `/api/v1/forecast` and `/api/v1/forecast-plans` endpoints | `backend/app/routers/forecast.py`, `forecast_plans.py` | 403 via `require_feature("forecast.enabled")` |

Note: `recurring.py` has no forecast-specific surfaces. Recurring templates feed the forecast engine but the recurring page is not forecast-specific. No gate needed there.

### Budget (5 gates)

| # | Location | File | Gate type |
|---|---|---|---|
| B1 | Sidebar nav "Budgets" item | `frontend/components/AppShell.tsx:68-71` | UI hide |
| B2 | `/budgets` page | `frontend/app/budgets/page.tsx:20` | redirect to `/dashboard` when flag off |
| B3 | Dashboard "Budget Progress" section | `frontend/app/dashboard/page.tsx:981-1037` | UI hide + skip fetch |
| B4 | "From Forecast" button on `/budgets` (cross-feature) | `frontend/app/budgets/page.tsx:215-221` | conditional: only show when both forecast and budget enabled |
| B5 | All `/api/v1/budgets` endpoints | `backend/app/routers/budgets.py` | 403 via `require_feature("budgets.enabled")` |

### Cross-feature gate

`POST /api/v1/budgets/from-forecast` reads forecast plan data. When forecast is disabled but budgets are enabled, this endpoint must 403 with `forecast.enabled` as the gating reason (dual-check). Frontend button hidden when forecast off (B4 above).

## Data flow

```
Owner visits /settings/organization
  -> clicks "Enable Forecast" toggle (currently on)
  -> PATCH /api/v1/settings/features {"forecast.enabled": false}
    -> require_org_owner gate (403 if non-owner)
    -> upsert OrgFeatureOverride(org_id, "forecast.enabled", False)
    -> audit_service.record_audit_event(event_type="org.feature.set", ...)
    -> returns 200 {"forecast.enabled": false, "budgets.enabled": true}
  -> frontend updates local state
  -> next page load: /me returns features."forecast.enabled" = false
  -> AppShell hides "Forecast Plans" nav item
  -> /forecast-plans redirects to /dashboard
  -> dashboard skips forecast fetches, hides OnTrackTile + AccountMonthEndForecast + forecast chart

Any caller hits GET /api/v1/forecast-plans:
  -> require_feature("forecast.enabled") dep
  -> get_current_org_features (request.state cache, per-request, no Redis)
  -> features["forecast.enabled"] = false -> raise HTTPException(403, "feature_not_enabled")
```

## Backwards compatibility

No migration. Existing orgs have no `OrgFeatureOverride` rows for these new keys. The resolver hits the `plan_features` layer, where `PlanFeatures.model_validate({})` returns `forecast_enabled=True` and `budgets_enabled=True` from the `default=True` field spec. Both features remain on for every existing org until an explicit disable. This is the only safe rollout.

## Acceptance criteria

**Given** an org owner is logged in,
**when** they visit `/settings/organization`,
**then** they see a "Features" card with two toggles ("Enable Forecast" on, "Enable Budgets" on), visible only to the owner role.

**Given** the owner turns off "Enable Forecast",
**when** the PATCH request completes,
**then** the "Forecast Plans" sidebar nav item disappears, `/forecast-plans` redirects to `/dashboard`, and `GET /api/v1/forecast-plans` returns 403 for all org members.

**Given** `forecast.enabled = false`,
**when** the dashboard loads,
**then** OnTrackTile, AccountMonthEndForecast, and the forecast expense chart are not rendered and their API calls are not issued.

**Given** a non-owner (admin or member) is logged in,
**when** they PATCH `/api/v1/settings/features`,
**then** they receive 403.

**Given** `forecast.enabled = false`,
**when** the owner re-enables forecast,
**then** the `OrgFeatureOverride` row for `forecast.enabled` is deleted (back to plan default `True`), nav item reappears, and existing forecast plans are accessible as before.

**Given** an existing org with no override rows (pre-rollout),
**when** PR 1 ships,
**then** `has_feature(db, org_id, "forecast.enabled")` returns `True` and `has_feature(db, org_id, "budgets.enabled")` returns `True`.

**Given** `forecast.enabled = false` but `budgets.enabled = true`,
**when** a user visits `/budgets`,
**then** budgets are accessible but the "From Forecast" button is hidden, and `POST /api/v1/budgets/from-forecast` returns 403.

## Build sequence

### PR 1 — Catalog keys + resolver default (~50 LOC, no behavior change)

- Add `"forecast.enabled"` and `"budgets.enabled"` to `FeatureKey` Literal in `backend/app/auth/feature_catalog.py`.
- Add `forecast_enabled: StrictBool = Field(default=True, alias="forecast.enabled")` and `budgets_enabled: StrictBool = Field(default=True, alias="budgets.enabled")` to `PlanFeatures` with a comment explaining the non-standard `True` default.
- Add both keys to `FeatureKey` union in `frontend/lib/types.ts`.
- Add both keys to `FeatureMap` interface in `frontend/lib/types.ts` (if it exists as a concrete map type).
- Add both to `FEATURE_LABELS` in `frontend/lib/feature-catalog.ts`.
- Run `python scripts/regen_feature_catalog_fixture.py` to update `frontend/tests/fixtures/feature-catalog.json`.
- Add backend test: `canonicalize_features({})` returns `True` for both new keys.
- Verify drift-guard test passes.

### PR 2 — Backend gates + self-service settings endpoint (~120 LOC, depends on PR 1)

- Add `Depends(require_feature("forecast.enabled"))` to all handlers in `forecast.py` and `forecast_plans.py`.
- Add `Depends(require_feature("budgets.enabled"))` to all handlers in `budgets.py`.
- Add dual `forecast.enabled` check on `POST /budgets/from-forecast`.
- Add `GET /api/v1/settings/features` endpoint returning effective values for `forecast.enabled`, `budgets.enabled`.
- Add `PATCH /api/v1/settings/features` endpoint (owner-only, upsert/delete override, audit).
- Add `FeaturesToggleRequest` and `FeaturesStateResponse` schemas to `backend/app/schemas/settings.py`.
- Backend tests: owner toggle persists; non-owner 403; GET returns current state; re-toggle removes override row; cross-feature `from-forecast` gate; toggles persisted via override row not plan row.

### PR 3 — Frontend gates + Settings UI (~200 LOC, depends on PR 1 + PR 2)

- Expose `features: {"forecast.enabled": bool, "budgets.enabled": bool}` on `GET /me` backend response.
- Thread feature flags into `AppShell.tsx` via `useAuth()` and filter `navItems`.
- Add feature check + redirect to `frontend/app/forecast-plans/page.tsx` RSC page.
- Add feature check + redirect to `frontend/app/budgets/page.tsx`.
- Conditionally skip forecast fetches and render in `frontend/app/dashboard/page.tsx`.
- Conditionally skip budget fetches and hide Budget Progress section in dashboard.
- Add "Features" card with two immediate-mutation toggle switches to `frontend/app/settings/organization/page.tsx` (owner-only).
- Hide "From Forecast" button on `/budgets` when `forecast.enabled = false`.
- Frontend tests: toggles fire correct PATCH; nav items absent when flag off; dashboard sections absent when flags off.

### Useful side-effect of PR 1

After PR 1 ships, the admin override surface at `/admin/orgs/[id]` works for these keys immediately. Operators can disable forecast/budgets for any org from the admin panel before the self-service UI (PR 3) lands. Useful partial-deploy window.

## Risks

1. **Resolver default consistency.** `PlanFeatures` having mixed defaults (`True` for these two, `False` for `ai.*`) is intentional but unusual. Any future developer adding a new key must choose the correct default consciously. Document on the model.
2. **RSC pre-rendering for `/forecast-plans` and `/budgets`.** Both are RSC-rendered via `getServerSessionResult()`. The feature check in the RSC page must distinguish "feature off for org" (deterministic, redirect) from "backend transient failure" (already handled by `serverFetch`'s discriminated result — render the client island, do not redirect). A 403 from the forecast/budget endpoint when the flag is off is deterministic; a 5xx or timeout is transient.
3. **Per-request feature cache, no Redis TTL.** Feature state cached only within a single HTTP request (`request.state`). No cross-request caching. If an owner disables a feature, the next request for any user in that org sees the new state immediately on next DB read. Correct and simple. No invalidation work needed.
4. **Cross-feature endpoint `budgets.from-forecast`.** When forecast off but budgets on, must 403 with `forecast.enabled` as the gating reason. Frontend "From Forecast" button hidden (B4). Otherwise the endpoint would silently return an empty result.
5. **Plan-level feature control.** An admin could set `forecast.enabled = false` on a plan via `/system/plans`. All orgs on that plan lose forecast access. This is correct (plans are a superset control), but operator-facing documentation should note it.
6. **`FeatureKey` union type in frontend.** Adding new keys to `FeatureKey` in `types.ts` and `FEATURE_LABELS` in `feature-catalog.ts` must happen in PR 1 or the frontend drift-guard test fails in PR 3.

## Substrate assessment

The L4.11 `OrgFeatureOverride` table is designed for admin-set overrides with optional `expires_at`. Reusing it for owner self-service (no expiry, no note) is slightly overloaded but correct: a row exists when an override is active, no row means "use plan/default value". An alternative dedicated `org_feature_settings` table adds migration cost with no behavioral gain.

The `require_feature` dependency factory is exactly the right primitive for backend gating. The main thing to get right is the `default=True` field spec in `PlanFeatures` — the only non-obvious substrate change.

The admin override UI at `/admin/orgs/[id]` already supports these keys once PR 1 ships, which gives operators control before PR 3's self-service UI lands.
